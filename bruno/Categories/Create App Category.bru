meta {
  name: Create App Category
  type: http
  seq: 2
}

post {
  url: {{baseUrl}}/api/categories/apps
  body: json
  auth: none
}

body:json {
  {
    "appName": "Visual Studio Code",
    "category": "development",
    "bundleId": "com.microsoft.VSCode"
  }
}
